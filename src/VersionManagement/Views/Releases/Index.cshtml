@using DD.Cloud.VersionManagement.DataAccess.Models
@model IEnumerable<ReleaseData>

@{
	ViewBag.Title = "Releases";
}

<div class="row">
	<div class="col-md-8">
		<table class="table">
			<thead>
				<tr>
					<th>Name</th>
					<th>Product</th>
					<th>Version range</th>
					<th>Special version</th>
					<th>Links</th>
				</tr>
			</thead>
			<tbody>
				@foreach (ReleaseData release in Model)
				{
					<tr>
						<td>
							<a asp-action="DetailById" asp-route-releaseId="@release.Id">@release.Name</a>
						</td>
						<td>
							<a asp-action="DetailById" asp-controller="Products" asp-route-productId="@release.ProductId">@release.Product.Name</a>
						</td>
						<td>
							<a asp-action="DetailById" asp-controller="VersionRange" asp-route-productId="@release.VersionRangeId">@release.VersionRange.Name</a>
						</td>
						<td>
							@release.SpecialVersion
						</td>
						<td>
							<a asp-action="Edit" asp-route-releaseId="@release.Id">Edit</a>
						</td>
					</tr>
				}
			</tbody>
		</table>
	</div>
</div>
<div class="row">
	<div class="col-md-8 text-right">
		<a asp-action="Create" class="btn btn-default">Create new release</a>
	</div>
</div>